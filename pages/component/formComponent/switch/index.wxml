<pageContainer headerTitle="switch">
	<view>
		<view class="switchView">
			<switch
				class=""
				checked="{{ checked }}"
				type="{{ type }}"
				disabled="{{ disabled }}"
				bindchange="onSwitchChange"
			>
			</switch>
		</view>
	</view>
</pageContainer>
