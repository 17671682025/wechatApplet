<pageContainer headerTitle="image">
	<view>
		<view>
			<image
				class="image"
				src="{{ src }}"
				mode="{{ mode }}"
				lazy-load="{{ lazyLoad }}"
				show-menu-by-longpress="{{ showMenuByLongPress }}"
			/>
		</view>
	</view>
</pageContainer>
